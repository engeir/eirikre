{{- $pctx := . -}}
{{- if .IsHome -}}{{ $pctx = .Site }}{{- end -}}
{{- $pages := slice -}}
{{- if or $.IsHome $.IsSection -}}
  {{- $pages = $pctx.RegularPages -}}
{{- else -}}
  {{- $pages = $pctx.Pages -}}
{{- end -}}
{{- $limit := .Site.Config.Services.RSS.Limit -}}
{{- if ge $limit 1 -}}
  {{- $pages = $pages | first $limit -}}
{{- end -}}
{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>{{ if eq .Title .Site.Title }}{{ .Site.Title }}{{ else }}{{ with .Title }}{{ . }} on {{ end }}{{ .Site.Title }}{{ end }}</title>
    <link>{{ .Permalink }}</link>
    <description>Recent content {{ if ne .Title .Site.Title }}in {{ .Title }} {{ end }}on {{ .Site.Title }}</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>{{ site.Language.LanguageCode }}</language>
    {{ with .Site.Copyright }}<copyright>{{ . }}</copyright>{{ end }}
    {{- if not .Date.IsZero }}
    <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>
    {{- end }}
    {{- with .OutputFormats.Get "RSS" }}
    <atom:link href="{{ .Permalink }}" rel="self" type="application/rss+xml" />
    {{- end }}
    {{ range $pages }}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      <guid>{{ .Permalink }}</guid>
      {{ with .Description }}<description>{{ . | html }}</description>{{ else }}<description>{{ .Summary | html }}</description>{{ end }}
      {{- $baseURL := strings.TrimSuffix "/" .Site.BaseURL -}}
      {{- $content := .Content -}}
      {{- /* 1. Strip LQIP base64 srcset (tiny blurry placeholder — confuses RSS readers) */}}
      {{- $content = $content | replaceRE ` srcset="data:[^"]*"` `` -}}
      {{- /* 2. Promote data-srcset → srcset (the real responsive image list) */}}
      {{- $content = $content | replaceRE ` data-srcset="` ` srcset="` -}}
      {{- /* 3. Remove data-sizes (non-standard, unneeded in RSS) */}}
      {{- $content = $content | replaceRE ` data-sizes="[^"]*"` `` -}}
      {{- /* 4. Make root-relative src/href/poster URLs absolute */}}
      {{- $content = $content | replaceRE `(src|href|poster)="(/[^"]*)"` (printf `$1="%s$2"` $baseURL) -}}
      {{- /* 5. Make srcset item URLs absolute (/path/img.webp 480w → https://…/path/img.webp 480w) */}}
      {{- $content = $content | replaceRE `(/[a-zA-Z0-9_./-]+ \d+w)` (printf `%s$1` $baseURL) -}}
      <content:encoded>{{ $content | html }}</content:encoded>
    </item>
    {{ end }}
  </channel>
</rss>
